# 核心集选择项目 Bug 修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复核心集选择实证研究项目中的所有已知bug，包括索引跟踪、模型维度、算法实现等关键问题

**Architecture:** 基于原始GitHub仓库(zalanborsos/bilevel_coresets, MingruiLiu-ML-Lab/Bilevel-Coreset-Selection-via-Regularization, RuilinTong/CSReL-Coreset-CL)的正确实现，修复我们的实现中的理论和工程错误

**Tech Stack:** PyTorch 2.0+, NumPy, YAML, 持续学习框架

---

## Bug 分类总览

### 🔴 严重级别 (Critical - 必须修复)
1. **索引跟踪系统崩溃** - `_parse_batch` 生成非唯一索引
2. **SimpleCNN MNIST维度不匹配** - `64*8*8` 假设32×32输入
3. **CSReL算法错误** - 缺少参考模型、错误的选择策略
4. **BCSR双层优化错误** - 缺少隐式梯度计算、正则化

### 🟡 中等级别 (Medium - 应该修复)
5. **权重对齐问题** - `train_task` 中权重未正确对齐到批次
6. **损失计算错误** - `compute_reducible_loss` 返回标量而非每样本损失

### 🟢 轻微级别 (Minor - 优化)
7. **特征提取回退逻辑** - Greedy方法的`_get_intermediate_features`

---

## Task 1: 修复索引跟踪系统

**Files:**
- Modify: [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py:12-30)

**问题描述:**
当前`_parse_batch`函数对于2元素批次(x, y)返回`torch.arange(x.size(0))`作为索引，这在多批场景下产生重复索引(每批都是从0开始)，导致索引映射完全错误。

- [ ] **Step 1: 读取当前的 `_parse_batch` 函数**

Run: `Read "f:/paper/coreset-empirical-study/core/coreset_base.py" lines 12-30`
Expected: 查看当前实现

- [ ] **Step 2: 修改 `_parse_batch` 函数以追踪全局索引计数器**

Edit [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py):

```python
# 在文件顶部添加全局索引计数器
_batch_index_counter = 0

def _parse_batch(batch):
    """
    解析数据批次，支持多种返回格式

    Args:
        batch: 数据批次，可能是 (x, y) 或 (x, y, idx)

    Returns:
        (x, y, indices): 数据、标签和索引
    """
    global _batch_index_counter

    if len(batch) == 3:
        x, y, indices = batch
    elif len(batch) == 2:
        x, y = batch
        # 生成全局唯一索引
        batch_size = x.size(0)
        indices = torch.arange(_batch_index_counter, _batch_index_counter + batch_size)
        _batch_index_counter += batch_size
    else:
        raise ValueError(f"Unexpected batch format: {len(batch)} elements")

    return x, y, indices
```

- [ ] **Step 3: 添加索引重置函数用于新实验开始时**

在 `core/coreset_base.py` 中添加:

```python
def reset_batch_index_counter():
    """重置全局批次索引计数器"""
    global _batch_index_counter
    _batch_index_counter = 0
```

- [ ] **Step 4: 在 `ExperimentRunner.run_single_experiment` 中调用重置函数**

Edit [`experiments/run_comparison.py`](f:/paper/coreset-empirical-study/experiments/run_comparison.py:193-201):

在创建模型之前添加:
```python
# 重置批次索引计数器
from core.coreset_base import reset_batch_index_counter
reset_batch_index_counter()
```

- [ ] **Step 5: 测试索引修复**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
from core.coreset_base import _parse_batch, reset_batch_index_counter
import torch

# 模拟两个批次
reset_batch_index_counter()
batch1 = (torch.randn(64, 3, 32, 32), torch.randint(0, 10, (64,)))
batch2 = (torch.randn(64, 3, 32, 32), torch.randint(0, 10, (64,)))

x1, y1, idx1 = _parse_batch(batch1)
x2, y2, idx2 = _parse_batch(batch2)

print(f'Batch 1 indices: {idx1[0].item()} to {idx1[-1].item()}')
print(f'Batch 2 indices: {idx2[0].item()} to {idx2[-1].item()}')
assert idx1[-1].item() + 1 == idx2[0].item(), '索引应该是连续的'
print('✓ 索引跟踪测试通过')
"
```

Expected output:
```
Batch 1 indices: 0 to 63
Batch 2 indices: 64 to 127
✓ 索引跟踪测试通过
```

- [ ] **Step 6: 提交索引跟踪修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/coreset_base.py experiments/run_comparison.py
git commit -m "fix: 修复批次索引跟踪系统，使用全局计数器生成唯一索引"
```

---

## Task 2: 修复 SimpleCNN MNIST 维度不匹配

**Files:**
- Modify: [`experiments/run_comparison.py`](f:/paper/coreset-empirical-study/experiments/run_comparison.py:32-62)

**问题描述:**
SimpleCNN的classifier使用硬编码的`64 * 8 * 8 = 4096`，这假设输入是32×32(CIFAR)，但MNIST是28×28，两次MaxPool2d后特征图是7×7而非8×8，导致维度不匹配。

- [ ] **Step 1: 查看当前 SimpleCNN 定义**

Run: `Read "f:/paper/coreset-empirical-study/experiments/run_comparison.py" lines 32-62`

- [ ] **Step 2: 添加自适应特征维度计算**

Edit [`experiments/run_comparison.py`](f:/paper/coreset-empirical-study/experiments/run_comparison.py:32-62):

```python
class SimpleCNN(nn.Module):
    """轻量 CNN，适用于 MNIST / CIFAR-10"""
    def __init__(self, num_classes=10, input_channels=1, input_size=28):
        """
        Args:
            num_classes: 分类类别数
            input_channels: 输入通道数 (1=灰度, 3=RGB)
            input_size: 输入图像尺寸 (MNIST=28, CIFAR=32)
        """
        super().__init__()
        self.input_size = input_size

        self.features = nn.Sequential(
            nn.Conv2d(input_channels, 32, 3, padding=1),
            nn.BatchNorm2d(32),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(2),
        )

        # 计算特征图尺寸: input_size -> Pool1 -> Pool2
        # 每次MaxPool2d(2)使尺寸减半
        feature_size = input_size // 4  # 两次MaxPool
        self.feature_dim = 64 * feature_size * feature_size

        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(self.feature_dim, 128),
            nn.ReLU(inplace=True),
            nn.Dropout(0.3),
            nn.Linear(128, num_classes),
        )

    def forward(self, x):
        x = self.features(x)
        x = self.classifier(x)
        return x

    def get_features(self, x):
        x = self.features(x)
        return x.view(x.size(0), -1)
```

- [ ] **Step 3: 更新 `create_model` 函数以传递正确的输入尺寸**

Edit [`experiments/run_comparison.py`](f:/paper/coreset-empirical-study/experiments/run_comparison.py:113-123):

```python
def create_model(dataset_name, num_classes):
    """根据数据集创建对应模型"""
    if dataset_name == 'mnist':
        return SimpleCNN(num_classes=num_classes, input_channels=1, input_size=28)
    elif dataset_name == 'cifar10':
        return SimpleCNN(num_classes=num_classes, input_channels=3, input_size=32)
    elif dataset_name == 'cifar100':
        return ResNetLike(num_classes=num_classes, input_channels=3)
    else:
        return SimpleCNN(num_classes=num_classes, input_channels=3, input_size=32)
```

- [ ] **Step 4: 测试模型维度**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import torch
from experiments.run_comparison import create_model

# 测试 MNIST
model_mnist = create_model('mnist', 10)
x_mnist = torch.randn(2, 1, 28, 28)
y_mnist = model_mnist(x_mnist)
print(f'MNIST input: {x_mnist.shape}, output: {y_mnist.shape}')
print(f'MNIST feature_dim: {model_mnist.feature_dim}')

# 测试 CIFAR-10
model_cifar = create_model('cifar10', 10)
x_cifar = torch.randn(2, 3, 32, 32)
y_cifar = model_cifar(x_cifar)
print(f'CIFAR input: {x_cifar.shape}, output: {y_cifar.shape}')
print(f'CIFAR feature_dim: {model_cifar.feature_dim}')

assert y_mnist.shape == (2, 10), 'MNIST输出维度错误'
assert y_cifar.shape == (2, 10), 'CIFAR输出维度错误'
print('✓ 模型维度测试通过')
"
```

Expected output:
```
MNIST input: torch.Size([2, 1, 28, 28]), output: torch.Size([2, 10])
MNIST feature_dim: 3136
CIFAR input: torch.Size([2, 3, 32, 32]), output: torch.Size([2, 10])
CIFAR feature_dim: 4096
✓ 模型维度测试通过
```

- [ ] **Step 5: 提交模型维度修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add experiments/run_comparison.py
git commit -m "fix: 修复SimpleCNN对MNIST的维度不匹配，添加自适应特征维度计算"
```

---

## Task 3: 修复 CSReL 算法实现

**Files:**
- Modify: [`core/methods/csrel.py`](f:/paper/coreset-empirical-study/core/methods/csrel.py)

**问题描述:**
根据原始仓库RuilinTong/CSReL-Coreset-CL的分析，当前CSReL实现存在以下严重错误：
1. 缺少参考模型训练(在全量数据上训练的理想模型)
2. Reducible Loss计算错误(应该是`L_current - L_reference`，而非只用当前模型损失)
3. 选择策略错误(应该是确定性Top-K，而非Softmax+随机采样)
4. 缺少增量式选择流程(应该分多轮，每轮选择少量样本)

- [ ] **Step 1: 备份当前CSReL实现**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && cp core/methods/csrel.py core/methods/csrel_old.py
```

- [ ] **Step 2: 完全重写 CSReL 实现**

Write [`core/methods/csrel.py`](f:/paper/coreset-empirical-study/core/methods/csrel.py):

```python
"""
CSReL: 基于可约损失的核心集选择
原始实现: https://github.com/RuilinTong/CSReL-Coreset-CL
"""
import torch
import torch.nn as nn
import torch.nn.functional as F
import numpy as np
from ..coreset_base import CoresetSelector, _parse_batch


class CSReLSelector(CoresetSelector):
    """
    CSReL (Coreset Selection via Reducible Loss)

    基于可约损失进行核心集选择

    Reducible Loss (ReL) = Loss(当前模型) - Loss(参考模型)

    - 参考模型: 在全量数据上训练的"理想模型"
    - ReL 高: 说明样本包含当前模型缺失的重要信息
    - 选择策略: 选择 ReL 最高的 Top-K 样本(确定性)

    参数:
        memory_budget: 核心集大小
        device: 计算设备
        num_incremental_steps: 增量选择步数(每步选择 memory_budget/num_steps 个样本)
        init_ratio: 初始随机选择比例
        class_balanced: 是否类别平衡
    """

    def __init__(
        self,
        memory_budget: int,
        device=None,
        num_incremental_steps: int = 5,
        init_ratio: float = 0.1,
        class_balanced: bool = False
    ):
        super().__init__(memory_budget, device)
        self.num_incremental_steps = num_incremental_steps
        self.init_ratio = init_ratio
        self.class_balanced = class_balanced
        self.reference_model = None
        self.ref_losses = None
        self.reloss_history = []

    def select_coreset(
        self,
        dataset,
        model,
        task_id: int,
        previous_coresets=None
    ):
        """
        基于可约损失选择核心集

        流程:
        1. 训练/加载参考模型(在全量数据上)
        2. 计算参考模型的损失
        3. 初始化: 随机选择少量样本
        4. 增量式选择:
           a. 在当前核心集上训练模型
           b. 计算可约损失 = 当前损失 - 参考损失
           c. 选择 ReL 最高的样本加入核心集
        5. 与历史核心集合并
        """
        model.eval()

        # 第一步: 提取所有数据和标签
        all_data, all_targets, all_indices = self._extract_full_dataset(dataset)
        num_samples = len(all_targets)

        if num_samples <= self.memory_budget:
            selected_indices = list(range(num_samples))
            weights = torch.ones(num_samples, device=self.device)
            self.selected_indices = selected_indices
            self.selection_weights = weights
            return selected_indices, weights

        # 第二步: 训练参考模型(首次或模型变化时)
        if self.reference_model is None or not self._is_same_architecture(model, self.reference_model):
            self.reference_model = self._train_reference_model(
                all_data, all_targets, model
            )
            # 计算参考损失
            self.ref_losses = self._compute_losses(
                self.reference_model, all_data, all_targets
            )

        # 第三步: 初始化核心集(随机选择少量样本)
        init_size = max(1, int(self.memory_budget * self.init_ratio))
        selected_indices = np.random.choice(
            num_samples, size=init_size, replace=False
        ).tolist()

        incremental_size = (self.memory_budget - init_size) // self.num_incremental_steps

        # 第四步: 增量式选择
        current_model = self._copy_model(model)

        for step in range(self.num_incremental_steps):
            # 在当前核心集上训练模型
            current_model = self._train_on_coreset(
                current_model, all_data[selected_indices], all_targets[selected_indices]
            )

            # 计算当前损失
            cur_losses = self._compute_losses(
                current_model, all_data, all_targets
            )

            # 计算可约损失
            rel_losses = cur_losses - self.ref_losses

            # 从未选样本中选择 ReL 最高的
            mask = torch.ones(num_samples, dtype=torch.bool, device=self.device)
            mask[selected_indices] = False

            if self.class_balanced:
                # 类别平衡选择
                new_indices = self._select_class_balanced(
                    rel_losses, all_targets, mask,
                    incremental_size
                )
            else:
                # 直接选择 ReL 最高的
                masked_rel = rel_losses.clone()
                masked_rel[~mask] = -float('inf')
                _, new_pos = torch.topk(masked_rel, incremental_size)
                new_indices = new_pos.tolist()

            selected_indices.extend(new_indices)

        # 确保不超过预算
        selected_indices = selected_indices[:self.memory_budget]

        # 计算权重(与ReL成正比)
        rel_final = (self._compute_losses(current_model, all_data, all_targets)
                     - self.ref_losses)
        weights = rel_final[selected_indices]

        # 归一化权重
        weights = weights / weights.sum()

        # 第五步: 与历史核心集合并
        if previous_coresets is not None and len(previous_coresets) > 0:
            selected_indices, weights = self._merge_with_previous(
                selected_indices, weights, previous_coresets,
                all_data, all_targets
            )

        self.selected_indices = selected_indices
        self.selection_weights = weights
        self.reloss_history.append(rel_losses.mean().item())

        return selected_indices, weights

    def _extract_full_dataset(self, dataset):
        """提取数据集中的所有数据、标签和索引"""
        all_data = []
        all_targets = []
        all_indices = []

        for batch in dataset:
            x, y, idx = _parse_batch(batch)
            all_data.append(x)
            all_targets.append(y)
            all_indices.extend(idx.tolist())

        all_data = torch.cat(all_data, dim=0).to(self.device)
        all_targets = torch.cat(all_targets, dim=0).to(self.device)

        return all_data, all_targets, all_indices

    def _train_reference_model(self, all_data, all_targets, base_model):
        """在全量数据上训练参考模型"""
        import copy

        ref_model = copy.deepcopy(base_model)
        ref_model.train()

        optimizer = torch.optim.SGD(
            ref_model.parameters(), lr=0.01, momentum=0.9
        )

        # 训练10个epoch
        for epoch in range(10):
            # 小批量训练
            batch_size = 128
            for i in range(0, len(all_data), batch_size):
                batch_x = all_data[i:i+batch_size]
                batch_y = all_targets[i:i+batch_size]

                optimizer.zero_grad()
                logits = ref_model(batch_x)
                loss = F.cross_entropy(logits, batch_y)
                loss.backward()
                optimizer.step()

        ref_model.eval()
        return ref_model.to(self.device)

    def _compute_losses(self, model, all_data, all_targets):
        """计算模型在所有样本上的损失"""
        model.eval()
        losses = []

        with torch.no_grad():
            batch_size = 128
            for i in range(0, len(all_data), batch_size):
                batch_x = all_data[i:i+batch_size]
                batch_y = all_targets[i:i+batch_size]

                logits = model(batch_x)
                loss = F.cross_entropy(logits, batch_y, reduction='none')
                losses.append(loss)

        return torch.cat(losses)

    def _train_on_coreset(self, model, coreset_data, coreset_targets):
        """在核心集上训练模型"""
        import copy

        model_copy = copy.deepcopy(model)
        model_copy.train()

        optimizer = torch.optim.SGD(
            model_copy.parameters(), lr=0.01, momentum=0.9
        )

        # 训练5个epoch
        for epoch in range(5):
            optimizer.zero_grad()
            logits = model_copy(coreset_data)
            loss = F.cross_entropy(logits, coreset_targets)
            loss.backward()
            optimizer.step()

        model_copy.eval()
        return model_copy.to(self.device)

    def _copy_model(self, model):
        """深拷贝模型"""
        import copy
        model_copy = copy.deepcopy(model)
        return model_copy.to(self.device)

    def _is_same_architecture(self, model1, model2):
        """检查两个模型是否架构相同"""
        return type(model1) == type(model2)

    def _select_class_balanced(self, rel_losses, all_targets, mask, k):
        """类别平衡的选择策略"""
        num_classes = all_targets.max().item() + 1
        per_class = k // num_classes

        selected = []

        for cls in range(num_classes):
            class_mask = (all_targets == cls) & mask
            class_rel = rel_losses.clone()
            class_rel[~class_mask] = -float('inf')

            _, top_pos = torch.topk(class_rel, per_class)
            selected.extend(top_pos.tolist())

        # 剩余随机选择
        remaining = k - len(selected)
        if remaining > 0:
            masked_rel = rel_losses.clone()
            masked_rel[~mask] = -float('inf')
            # 排除已选的
            masked_rel[selected] = -float('inf')
            _, top_pos = torch.topk(masked_rel, remaining)
            selected.extend(top_pos.tolist())

        return selected

    def _merge_with_previous(
        self, selected_indices, weights, previous_coresets,
        all_data, all_targets
    ):
        """与历史核心集合并"""
        all_previous = []
        for prev in previous_coresets:
            all_previous.extend(prev)

        budget_hist = self.memory_budget // 2
        budget_new = self.memory_budget - budget_hist

        if len(all_previous) > budget_hist:
            # 评估历史样本重要性
            hist_losses = self._compute_losses(
                self.reference_model, all_data[all_previous], all_targets[all_previous]
            )
            _, hist_top = torch.topk(hist_losses, budget_hist)
            hist_selected = [all_previous[i] for i in hist_top.tolist()]
        else:
            hist_selected = all_previous

        new_selected = selected_indices[:budget_new]
        final_indices = hist_selected + new_selected

        # 重新计算权重
        final_weights = torch.zeros(len(final_indices), device=self.device)
        final_weights[:len(hist_selected)] = 1.2  # 历史权重略高
        final_weights[len(hist_selected):] = 0.8   # 新样本权重略低
        final_weights = final_weights / final_weights.sum()

        return final_indices, final_weights
```

- [ ] **Step 3: 测试修复后的CSReL**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import torch
from core import get_selector
from data.datasets import ContinualDataset

device = torch.device('cpu')
dataset = ContinualDataset('mnist', batch_size=64, num_workers=0)
dataset.split_tasks(2, 'split')

model = torch.nn.Sequential(
    torch.nn.Flatten(),
    torch.nn.Linear(784, 128),
    torch.nn.ReLU(),
    torch.nn.Linear(128, 10)
).to(device)

selector = get_selector('csrel', memory_budget=100, device=device)
train_loader = dataset.get_task_loaders(0)

print('测试CSReL选择...')
indices, weights = selector.select_coreset(train_loader, model, task_id=0)
print(f'选择 {len(indices)} 个样本')
print(f'权重范围: [{weights.min():.4f}, {weights.max():.4f}]')
print(f'权重和: {weights.sum():.4f}')
print('✓ CSReL测试通过')
"
```

Expected output:
```
测试CSReL选择...
选择 100 个样本
权重范围: [0.00XX, 0.0XXX]
权重和: 1.0000
✓ CSReL测试通过
```

- [ ] **Step 4: 提交CSReL修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/methods/csrel.py
git commit -m "fix: 完全重写CSReL实现，符合原始论文算法"
```

---

## Task 4: 修复 BCSR 双层优化实现

**Files:**
- Modify: [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py)

**问题描述:**
根据原始仓库zalanborsos/bilevel_coresets和MingruiLiu-ML-Lab/Bilevel-Coreset-Selection-via-Regularization的分析：
1. 缺少正确的隐式梯度计算(通过隐函数定理)
2. 缺少Top-K正则化(-β·sum(topk_weights))
3. 缺少Simplex投影(约束权重和为1)
4. 当前手动设置梯度不符合双层优化理论

- [ ] **Step 1: 备份当前BCSR实现**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && cp core/methods/bcsr.py core/methods/bcsr_old.py
```

- [ ] **Step 2: 添加隐式梯度计算函数**

在 [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py) 中添加:

```python
def _implicit_gradient(self, model, train_data, train_targets, val_data, val_targets, weights):
    """
    计算双层优化的隐式梯度

    使用隐函数定理: ∂L_val/∂w = ∂L_val/∂θ · (∂²L_train/∂θ²)^(-1) · ∂²L_train/∂θ∂w

    通过一阶近似避免计算完整的Hessian矩阵
    """
    # 1. 外层损失对模型参数的梯度
    val_pred = model(val_data)
    val_loss = F.cross_entropy(val_pred, val_targets)
    d_theta = torch.autograd.grad(val_loss, model.parameters(), retain_graph=True)
    d_theta_flat = torch.cat([g.flatten() for g in d_theta if g is not None])

    # 2. 内层损失对模型参数的梯度(需要二阶导数)
    train_pred = model(train_data)
    sample_losses = F.cross_entropy(train_pred, train_targets, reduction='none')
    weighted_loss = (sample_losses * weights).sum()

    grads_theta = torch.autograd.grad(
        weighted_loss, model.parameters(), create_graph=True
    )

    # 3. 近似 Hessian-vector 乘积
    # 使用 θ - lr·∇θ 近似 SGD 一步后的参数
    G_theta = []
    for p, g in zip(model.parameters(), grads_theta):
        if g is not None:
            G_theta.append(p - self.inner_lr * g)
        else:
            G_theta.append(p)

    # 4. 多次迭代近似 H^(-1) * v
    v_Q = [g.detach() for g in d_theta]
    for _ in range(3):
        v_new = torch.autograd.grad(
            G_theta, model.parameters(),
            grad_outputs=v_Q, retain_graph=True
        )
        v_new = [v.detach() if v is not None else torch.zeros_like(p)
                 for v, p in zip(v_new, model.parameters())]
        for i in range(len(v_Q)):
            if v_Q[i] is not None:
                v_Q[i].add_(v_new[i])

    # 5. 计算 Jacobian (权重梯度)
    jacobian_grads = torch.autograd.grad(
        grads_theta, weights, grad_outputs=v_Q
    )

    jacobian = -jacobian_grads[0]  # 负号很重要!

    return jacobian
```

- [ ] **Step 3: 添加 Top-K 正则化**

在 [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py) 中添加:

```python
def _topk_regularization(self, weights, beta=1.0, topk=None):
    """
    Top-K 正则化: -β * sum(topk_weights)

    激励只有 K 个样本有大权重，促进稀疏性
    """
    if topk is None:
        topk = self.memory_budget

    topk_weights, _ = weights.topk(topk)

    # 添加平滑噪声
    epsilon = 1e-3
    noise = torch.normal(0, 1, size=[topk], device=weights.device)

    regularizer = -beta * (topk_weights + epsilon * noise).sum()

    return regularizer
```

- [ ] **Step 4: 添加 Simplex 投影**

在 [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py) 中添加:

```python
def _projection_onto_simplex(self, v, b=1.0):
    """
    投影到单纯形: Σw_i = b, w_i ≥ 0

    使用 Duchi et al. (2008) 的算法
    """
    v = v.detach().cpu().numpy()
    n_features = v.shape[0]

    # 排序(降序)
    u = np.sort(v)[::-1]

    # 计算累积和
    cssv = np.cumsum(u) - b

    # 找到阈值
    ind = np.arange(n_features) + 1
    cond = u - cssv / ind > 0
    rho = ind[cond][-1]
    theta = cssv[cond][-1] / float(rho)

    # 投影
    w = np.maximum(v - theta, 0)

    # 转回 tensor
    w = torch.from_numpy(w).to(self.device)

    return w
```

- [ ] **Step 5: 修改 `select_coreset` 方法使用正确的双层优化**

Edit [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py:66-208) 的 `select_coreset` 方法:

```python
def select_coreset(self, dataset, model, task_id: int, previous_coresets=None):
    """使用正确的双层优化选择核心集"""
    model.eval()

    # 提取数据
    all_data, all_targets, all_indices = self._extract_data(dataset)
    num_samples = len(all_targets)

    if num_samples <= self.memory_budget:
        selected_indices = list(range(num_samples))
        weights = torch.ones(num_samples, device=self.device)
        self.selected_indices = selected_indices
        self.selection_weights = weights
        return selected_indices, weights

    # 划分训练/验证集
    if self.use_val_set:
        perm = torch.randperm(num_samples)
        val_size = int(num_samples * self.val_ratio)
        val_idx = perm[:val_size]
        train_idx = perm[val_size:]
    else:
        train_idx = torch.arange(num_samples)
        val_idx = torch.arange(num_samples)

    train_data = all_data[train_idx]
    train_targets = all_targets[train_idx]
    val_data = all_data[val_idx]
    val_targets = all_targets[val_idx]

    # 初始化选择权重
    n_train = train_data.shape[0]
    self.selection_logits = torch.zeros(
        n_train, device=self.device, requires_grad=True
    )

    # 使用Adam优化器
    optimizer = torch.optim.Adam([self.selection_logits], lr=self.outer_lr)

    for meta_step in range(self.meta_steps):
        # 获取选择概率
        selection_probs = torch.softmax(
            self.selection_logits / self.temperature, dim=0
        )

        # --- 内层优化: 在加权子集上训练模型 ---
        model_copy = self._copy_model(model)
        inner_opt = torch.optim.SGD(
            model_copy.parameters(), lr=self.inner_lr
        )
        detached_probs = selection_probs.detach()

        for _ in range(self.inner_steps):
            inner_opt.zero_grad()
            logits = model_copy(train_data)
            loss = F.cross_entropy(logits, train_targets, reduction='none')
            weighted_loss = (loss * detached_probs).sum()
            weighted_loss.backward()
            inner_opt.step()

        # --- 外层优化: 使用隐式梯度 ---
        jacobian = self._implicit_gradient(
            model_copy, train_data, train_targets,
            val_data, val_targets, selection_probs
        )

        # 梯度裁剪
        jacobian = jacobian.clamp(-1, 1)

        # Top-K 正则化
        regularizer = self._topk_regularization(
            selection_probs, beta=0.1, topk=self.memory_budget
        )

        # 更新权重
        optimizer.zero_grad()
        self.selection_logits.grad = jacobian + regularizer
        optimizer.step()

    # Simplex 投影
    final_probs = torch.softmax(
        self.selection_logits / self.temperature, dim=0
    )
    final_probs = self._projection_onto_simplex(final_probs)

    # 选择 top-k 或采样
    k = min(self.memory_budget, n_train)
    _, top_k_pos = torch.topk(final_probs, k)

    # 映射回原始索引
    selected_indices = [all_indices[train_idx[i].item()] for i in top_k_pos.tolist()]
    weights = final_probs[top_k_pos].detach()

    # 与历史核心集合并
    if previous_coresets is not None and len(previous_coresets) > 0:
        selected_indices, weights = self._merge_with_previous(
            selected_indices, weights, previous_coresets,
            all_data, all_targets, all_indices
        )

    self.selected_indices = selected_indices
    self.selection_weights = weights

    return selected_indices, weights
```

- [ ] **Step 6: 添加 `_merge_with_previous` 方法**

在 [`core/methods/bcsr.py`](f:/paper/coreset-empirical-study/core/methods/bcsr.py) 中添加:

```python
def _merge_with_previous(
    self, selected_indices, weights, previous_coresets,
    all_data, all_targets, all_indices
):
    """与历史核心集合并"""
    all_previous = []
    for prev in previous_coresets:
        all_previous.extend(prev)

    budget_hist = self.memory_budget // 2
    budget_new = self.memory_budget - budget_hist

    if len(all_previous) > budget_hist:
        # 使用隐式梯度评估历史样本重要性
        hist_importance = self._evaluate_importance(
            all_data, all_targets, all_indices, all_previous
        )
        _, hist_top = torch.topk(hist_importance, budget_hist)
        hist_selected = [all_previous[i] for i in hist_top.tolist()]
    else:
        hist_selected = all_previous

    new_selected = selected_indices[:budget_new]
    final_indices = hist_selected + new_selected
    weights = torch.cat([
        torch.ones(len(hist_selected), device=self.device) * 1.2,
        weights[:budget_new]
    ])
    weights = weights / weights.sum()

    return final_indices, weights
```

- [ ] **Step 7: 测试修复后的BCSR**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import torch
from core import get_selector
from data.datasets import ContinualDataset

device = torch.device('cpu')
dataset = ContinualDataset('mnist', batch_size=64, num_workers=0)
dataset.split_tasks(2, 'split')

model = torch.nn.Sequential(
    torch.nn.Flatten(),
    torch.nn.Linear(784, 128),
    torch.nn.ReLU(),
    torch.nn.Linear(128, 10)
).to(device)

selector = get_selector('bcsr', memory_budget=100, device=device,
                       meta_steps=2, inner_steps=3)
train_loader = dataset.get_task_loaders(0)

print('测试BCSR选择...')
indices, weights = selector.select_coreset(train_loader, model, task_id=0)
print(f'选择 {len(indices)} 个样本')
print(f'权重范围: [{weights.min():.4f}, {weights.max():.4f}]')
print(f'权重和: {weights.sum():.4f}')
print('✓ BCSR测试通过')
"
```

Expected output:
```
测试BCSR选择...
选择 100 个样本
权重范围: [0.00XX, 0.0XXX]
权重和: 1.0000
✓ BCSR测试通过
```

- [ ] **Step 8: 提交BCSR修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/methods/bcsr.py
git commit -m "fix: 实现正确的BCSR双层优化，包括隐式梯度、Top-K正则化、Simplex投影"
```

---

## Task 5: 修复权重对齐问题

**Files:**
- Modify: [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py:164-231)

**问题描述:**
`train_task`方法中`coreset_weights`是整个核心集的权重，但训练时需要将其正确对齐到当前批次的样本。当前实现直接将整个权重向量应用到批次，这在批次大小不等于核心集大小时会出错。

- [ ] **Step 1: 修改 `train_task` 方法以接受索引-权重映射**

Edit [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py:164-231):

```python
def train_task(
    self,
    train_loader: DataLoader,
    num_epochs: int = 50,
    coreset_indices: list = None,
    coreset_weights: torch.Tensor = None
) -> dict:
    """
    训练单个任务

    Args:
        train_loader: 训练数据加载器
        num_epochs: 训练轮数
        coreset_indices: 核心集索引
        coreset_weights: 核心集权重(必须与indices长度一致)

    Returns:
        训练指标字典
    """
    self.model.train()

    if self.optimizer is None:
        self.optimizer = self.optimizer_class(
            self.model.parameters(),
            **self.optimizer_kwargs
        )

    metrics = {
        'train_losses': [],
        'train_accuracy': []
    }

    criterion = nn.CrossEntropyLoss(reduction='none')

    for epoch in range(num_epochs):
        epoch_loss = 0.0
        correct = 0
        total = 0

        for batch in train_loader:
            batch_x, batch_y, batch_idx = _parse_batch(batch)
            batch_x = batch_x.to(self.device)
            batch_y = batch_y.to(self.device)

            self.optimizer.zero_grad()
            outputs = self.model(batch_x)

            # 计算每个样本的损失
            per_sample_loss = criterion(outputs, batch_y)

            # 如果提供了核心集权重，正确对齐
            if coreset_weights is not None and coreset_indices is not None:
                # 创建索引到权重的映射
                index_to_weight = {
                    idx: weight.item()
                    for idx, weight in zip(coreset_indices, coreset_weights)
                }

                # 获取当前批次中每个样本的权重
                batch_weights = torch.tensor(
                    [index_to_weight.get(idx.item(), 1.0)
                     for idx in batch_idx],
                    device=self.device, dtype=per_sample_loss.dtype
                )

                # 加权损失
                loss = (per_sample_loss * batch_weights).mean()
            else:
                loss = per_sample_loss.mean()

            loss.backward()
            self.optimizer.step()

            epoch_loss += loss.item() * batch_x.size(0)
            _, predicted = outputs.max(1)
            correct += predicted.eq(batch_y).sum().item()
            total += batch_x.size(0)

        avg_loss = epoch_loss / total
        accuracy = correct / total

        metrics['train_losses'].append(avg_loss)
        metrics['train_accuracy'].append(accuracy)

    return metrics
```

- [ ] **Step 2: 测试权重对齐**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import torch
from torch.utils.data import DataLoader, TensorDataset
from core.coreset_base import ContinualLearningFramework

# 创建简单数据
X = torch.randn(100, 10)
y = torch.randint(0, 2, (100,))
dataset = TensorDataset(X, y)
loader = DataLoader(dataset, batch_size=32, shuffle=False)

# 创建模型
model = torch.nn.Sequential(
    torch.nn.Linear(10, 5),
    torch.nn.ReLU(),
    torch.nn.Linear(5, 2)
)

# 创建框架
framework = ContinualLearningFramework(model, device=torch.device('cpu'))

# 测试带权重训练
indices = [0, 1, 2, 10, 11, 12]
weights = torch.tensor([0.5, 0.5, 0.5, 1.5, 1.5, 1.5])

metrics = framework.train_task(
    loader, num_epochs=1, coreset_indices=indices, coreset_weights=weights
)

print(f'训练损失: {metrics[\"train_losses\"][0]:.4f}')
print(f'训练准确率: {metrics[\"train_accuracy\"][0]:.4f}')
print('✓ 权重对齐测试通过')
"
```

Expected output:
```
训练损失: X.XXXX
训练准确率: X.XXXX
✓ 权重对齐测试通过
```

- [ ] **Step 3: 提交权重对齐修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/coreset_base.py
git commit -m "fix: 修复train_task中的权重对齐问题，正确处理批次与核心集索引的映射"
```

---

## Task 6: 修复 compute_reducible_loss 返回值

**Files:**
- Modify: [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py:96-135)

**问题描述:**
`compute_reducible_loss`函数当前返回每批次的标量损失，然后尝试concatenate这些标量，这是错误的。应该返回每个样本的损失。

- [ ] **Step 1: 查看当前实现**

Run: `Read "f:/paper/coreset-empirical-study/core/coreset_base.py" lines 96-135`

- [ ] **Step 2: 修改 `compute_reducible_loss` 以返回每样本损失**

Edit [`core/coreset_base.py`](f:/paper/coreset-empirical-study/core/coreset_base.py:96-135):

```python
def compute_reducible_loss(
    self,
    dataset: DataLoader,
    model_full: nn.Module,
    model_subset: nn.Module
) -> torch.Tensor:
    """
    计算可约损失 (用于CSReL方法)

    Reducible Loss = Loss(子集模型) - Loss(全量模型)

    Args:
        dataset: 数据集
        model_full: 在全量数据上训练的模型
        model_subset: 在子集上训练的模型

    Returns:
        每个样本的可约损失值
    """
    model_full.eval()
    model_subset.eval()

    rel_losses = []

    with torch.no_grad():
        for batch in dataset:
            batch_x, batch_y, _ = _parse_batch(batch)
            batch_x = batch_x.to(self.device)
            batch_y = batch_y.to(self.device)

            # 全量模型的每样本损失
            logits_full = model_full(batch_x)
            loss_full = nn.functional.cross_entropy(
                logits_full, batch_y, reduction='none'
            )

            # 子集模型的每样本损失
            logits_sub = model_subset(batch_x)
            loss_sub = nn.functional.cross_entropy(
                logits_sub, batch_y, reduction='none'
            )

            # 可约损失 = 子集损失 - 全量损失
            rel_loss = loss_sub - loss_full
            rel_losses.append(rel_loss)

    return torch.cat(rel_losses)
```

- [ ] **Step 3: 提交修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/coreset_base.py
git commit -m "fix: 修复compute_reducible_loss返回每样本损失而非标量"
```

---

## Task 7: 修复 Greedy 特征提取回退逻辑

**Files:**
- Modify: [`core/methods/greedy.py`](f:/paper/coreset-empirical-study/core/methods/greedy.py:125-143)

**问题描述:**
`_get_intermediate_features`的回退逻辑(当模型没有`get_features`方法时)会遍历所有子模块并在第一个Linear层处停止，但这可能返回分类器的输出(logits)而非中间特征。

- [ ] **Step 1: 查看当前实现**

Run: `Read "f:/paper/coreset-empirical-study/core/methods/greedy.py" lines 125-143`

- [ ] **Step 2: 改进回退逻辑**

Edit [`core/methods/greedy.py`](f:/paper/coreset-empirical-study/core/methods/greedy.py:125-143):

```python
def _get_intermediate_features(self, model, x):
    """
    获取模型中间层特征

    策略:
    1. 如果模型有 get_features 方法，使用它
    2. 否则，尝试找到特征提取层并使用它
    3. 最后回退到使用展平的原始输入
    """
    # 优先使用模型的 get_features 方法
    if hasattr(model, 'get_features'):
        return model.get_features(x)

    # 尝试找到特征提取层
    # 对于 Sequential 结构，找到最后的卷积层
    features = x
    found_features = False

    for name, module in model.named_children():
        if isinstance(module, torch.nn.Sequential):
            # 对于 Sequential，找到最后的非分类器部分
            for sub_name, sub_module in module.named_children():
                if isinstance(sub_module, (torch.nn.Conv2d, torch.nn.ReLU,
                                            torch.nn.BatchNorm2d, torch.nn.MaxPool2d)):
                    features = sub_module(features)
                    found_features = True
                elif isinstance(sub_module, (torch.nn.Linear, torch.nn.Flatten)):
                    # 到达分类器，停止
                    break
        elif not isinstance(module, (torch.nn.Linear, torch.nn.Flatten)):
            features = module(features)
            found_features = True

    # 如果找到了特征层，展平并返回
    if found_features:
        if features.dim() > 2:
            features = features.view(features.size(0), -1)
        return features

    # 最终回退: 展平原始输入
    print("Warning: 无法提取中间特征，使用原始输入")
    return x.view(x.size(0), -1)
```

- [ ] **Step 3: 测试特征提取**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import torch
from core.methods.greedy import GreedySelector

# 测试模型
model_with_features = torch.nn.Sequential(
    torch.nn.Conv2d(1, 16, 3, padding=1),
    torch.nn.ReLU(),
    torch.nn.Flatten(),
    torch.nn.Linear(16*28*28, 10)
)

selector = GreedySelector(memory_budget=10, device=torch.device('cpu'))
x = torch.randn(2, 1, 28, 28)

feat = selector._get_intermediate_features(model_with_features, x)
print(f'特征形状: {feat.shape}')
assert feat.dim() == 2, '特征应该是2维的'
print('✓ 特征提取测试通过')
"
```

Expected output:
```
特征形状: torch.Size([2, XXXX])
✓ 特征提取测试通过
```

- [ ] **Step 4: 提交修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add core/methods/greedy.py
git commit -m "fix: 改进Greedy方法的特征提取回退逻辑"
```

---

## Task 8: 全面验证修复

- [ ] **Step 1: 运行快速验证测试**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python run_quick.py
```

Expected: 所有方法(random, greedy, csrel, bcsr, ensemble)都应该通过测试

- [ ] **Step 2: 运行完整的 MNIST 实验**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python experiments/run_comparison.py \
    --dataset mnist \
    --method random greedy csrel bcsr \
    --quick \
    --output results/bug_fix_test
```

Expected: 实验应该成功完成，所有方法都能运行

- [ ] **Step 3: 检查结果合理性**

Run:
```bash
cd "f:/paper/coreset-empirical-study" && python -c "
import json
from pathlib import Path

result_file = Path('results/bug_fix_test')
json_files = list(result_file.glob('comparison_*.json'))

if json_files:
    with open(json_files[0], 'r') as f:
        results = json.load(f)

    for result in results:
        method = result['method']
        aa = result['summary']['average_accuracy']
        fm = result['summary']['forgetting_measure']
        print(f'{method:10s}: AA={aa:.4f}, FM={fm:.4f}')
else:
    print('未找到结果文件')
"
```

Expected output:
```
random     : AA=0.XXXX, FM=0.XXXX
greedy     : AA=0.XXXX, FM=0.XXXX
csrel      : AA=0.XXXX, FM=0.XXXX
bcsr       : AA=0.XXXX, FM=0.XXXX
```

- [ ] **Step 4: 如果所有测试通过，创建总结文档**

Write `BUG_FIX_SUMMARY.md`:

```markdown
# Bug 修复总结

## 修复的 Bug

### 1. 索引跟踪系统 ✓
- 问题: `_parse_batch` 生成重复索引
- 修复: 使用全局计数器生成唯一索引

### 2. SimpleCNN 维度匹配 ✓
- 问题: MNIST (28×28) 使用 CIFAR (32×32) 的维度
- 修复: 添加自适应特征维度计算

### 3. CSReL 算法 ✓
- 问题: 缺少参考模型、错误的选择策略
- 修复: 完全重写，符合原始论文算法

### 4. BCSR 双层优化 ✓
- 问题: 缺少隐式梯度、正则化、Simplex投影
- 修复: 实现完整的双层优化框架

### 5. 权重对齐 ✓
- 问题: 训练时权重未正确对齐到批次
- 修复: 使用索引-权重映射

### 6. 损失计算 ✓
- 问题: 返回标量而非每样本损失
- 修复: 使用 `reduction='none'`

### 7. 特征提取 ✓
- 问题: 回退逻辑可能返回 logits
- 修复: 改进特征提取策略

## 验证结果

所有方法在 MNIST 上运行正常:
- Random: AA=0.XXXX, FM=0.XXXX
- Greedy: AA=0.XXXX, FM=0.XXXX
- CSReL: AA=0.XXXX, FM=0.XXXX
- BCSR: AA=0.XXXX, FM=0.XXXX

## 下一步

可以开始完整的实证研究实验。
```

- [ ] **Step 5: 提交所有最终修复**

```bash
cd "f:/paper/coreset-empirical-study" && git add -A
git commit -m "docs: 添加bug修复总结文档"
```

---

## 修复验证清单

完成所有任务后，验证以下功能:

- [ ] 所有方法都能在 MNIST 上运行
- [ ] 所有方法都能在 CIFAR-10 上运行
- [ ] 索引映射正确(无重复索引)
- [ ] SimpleCNN 适用于所有数据集
- [ ] CSReL 使用参考模型和增量选择
- [ ] BCSR 使用隐式梯度和正则化
- [ ] 权重正确应用于训练
- [ ] 特征提取适用于所有模型类型

---

## 估计时间

- Task 1 (索引跟踪): 30 分钟
- Task 2 (模型维度): 20 分钟
- Task 3 (CSReL): 1.5 小时
- Task 4 (BCSR): 2 小时
- Task 5 (权重对齐): 30 分钟
- Task 6 (损失计算): 15 分钟
- Task 7 (特征提取): 30 分钟
- Task 8 (验证): 1 小时

**总计: 约 6-7 小时**
